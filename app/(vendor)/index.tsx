// app/(vendor)/index.tsx  (Vendor Home Screen)
import { View, Text } from "react-native";

export default function VendorHomeScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Vendor Home Screen</Text>
    </View>
  );
}
